[project]
name = "fashion-app"
version = "0.1.0"
description = "AI Fashion App"
requires-python = ">=3.11"

dependencies = [
    "streamlit"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
